# 派工系統操作教學

---

## 目錄

1. [系統管理員 — 初次部署](#一系統管理員--初次部署)
2. [主管 — 首次設定](#二主管--首次設定)
3. [主管 — 日常操作](#三主管--日常操作)
4. [員工 — 註冊與登入](#四員工--註冊與登入)
5. [員工 — 每日填寫派工](#五員工--每日填寫派工)
6. [常見問題](#六常見問題)

---

## 一、系統管理員 — 初次部署

> 此步驟只需做一次。完成後將網址、邀請碼交給主管即可。

### 1. 建立 Master Google Sheet

1. 前往 [Google Sheets](https://sheets.google.com)，登入要用來管理系統的 Google 帳號
2. 點選「**空白試算表**」新增一份空白的 Sheet
3. 命名為「派工系統資料庫」（名稱可自訂）
4. 記下網址列中的試算表 ID：

   ```
   https://docs.google.com/spreadsheets/d/【這裡就是 ID】/edit
   ```

### 2. 建立 Google Apps Script

1. 在試算表上方點選「**擴充功能 → Apps Script**」
2. 刪除編輯器中預設的 `function myFunction() {}` 全部內容
3. 將專案的 `gas/code.gs` 檔案內容完整貼上
4. 按「**儲存**」（Ctrl+S 或 Cmd+S）

### 3. 設定指令碼屬性

1. 點選左側的**齒輪圖示**（專案設定）
2. 捲到最下方，找到「**指令碼屬性**」，點選「**新增指令碼屬性**」
3. 依序加入以下兩筆屬性：

   | 屬性名稱 | 值 | 說明 |
   |---------|---|------|
   | `MASTER_SHEET_ID` | 步驟 1 記下的試算表 ID | |
   | `MANAGER_INVITE_CODE` | 自訂一組邀請碼 | 例如 `xingying2026`，主管註冊時需要輸入 |

4. 點選「**儲存指令碼屬性**」

### 4. 初始化資料分頁

1. 回到 Apps Script 編輯器
2. 上方函式下拉選單中，選擇 **`setupSheets`**
3. 按「**執行**」
4. 第一次執行會要求授權，點選「**審查權限**」→ 選擇該 Google 帳號 → 點「**允許**」
5. 執行完成後，回到 Google Sheet，應該會看到自動建立了四個分頁：
   - `Users`
   - `Sessions`
   - `WorkOrders`
   - `Reports`

### 5. 部署為網頁應用程式

1. 在 Apps Script 編輯器，點選右上角「**部署 → 新增部署**」
2. 類型選「**網頁應用程式**」
3. 填入設定：
   - 說明：`派工系統 API`（可自訂）
   - 執行身分：**我**
   - 存取權限：**所有人**
4. 點選「**部署**」，完成授權
5. 複製產生的 **Web App URL**（格式為 `https://script.google.com/macros/s/.../exec`）

> ⚠️ 之後若修改 `code.gs`，必須重新部署：**部署 → 管理部署 → 編輯 → 版本選「新版本」→ 部署**

### 6. 設定前端並上線

1. 開啟專案中的 `js/api.js`，將 `YOUR_GOOGLE_APPS_SCRIPT_URL` 替換為上一步的 URL：

   ```js
   const API_URL = 'https://script.google.com/macros/s/你的ID/exec';
   ```

2. 將整個專案推送到 GitHub Private Repository：

   ```bash
   git init
   git add .
   git commit -m "init"
   git branch -M main
   git remote add origin https://github.com/你的帳號/你的repo.git
   git push -u origin main
   ```

3. 前往 [Cloudflare Pages](https://dash.cloudflare.com)：
   - **Workers & Pages → Create → Pages → Connect to Git**
   - 選擇剛才的 repository
   - Build settings 全部**留空**
   - 點選「**Save and Deploy**」
   - 完成後取得網址，例如 `https://xingying-dispatch.pages.dev`

---

## 二、主管 — 首次設定

### 1. 建立主管帳號

1. 開啟系統網址，點選「**主管帳號申請**」
2. 填入以下資料：
   - 工號（自訂，例如 `M001`）
   - 姓名
   - 密碼（至少 6 個字元）
   - 確認密碼
   - 邀請碼（由系統管理員提供的 `MANAGER_INVITE_CODE`）
3. 點選「**建立帳號**」，成功後前往登入頁

### 2. 準備工單 Google Sheet

> 這份 Sheet 是用來管理「船號」和「工單號碼」對應關係的來源。

1. 新增一份 Google Sheet（可命名為「工單清單」）
2. **每個分頁代表一艘船**，分頁名稱就是船號

   範例結構：

   ```
   分頁名稱：海勝一號          分頁名稱：興盈二號
   ┌──────────────┐           ┌──────────────┐
   │ A1: 工單號碼  │           │ A1: 工單號碼  │
   │ A2: WO-001   │           │ A2: WO-010   │
   │ A3: WO-002   │           │ A3: WO-011   │
   │ A4: WO-003   │           │ A4: WO-012   │
   └──────────────┘           └──────────────┘
   ```

   注意事項：
   - 第 1 列為標題列（內容不限，系統從第 2 列開始讀取）
   - A 欄填工單號碼，每列一筆
   - 分頁名稱即為員工看到的船號選項

3. 將此 Google Sheet **分享**給系統使用的 Google 帳號（可檢視權限即可）：
   - 點選右上角「**共用**」
   - 輸入部署 GAS 的 Google 帳號 Email
   - 權限選「**檢視者**」→「**傳送**」

### 3. 匯入工單

1. 以主管帳號登入系統
2. 點選「**工單管理**」分頁
3. 在「從 Google Sheet 匯入工單」區塊：
   - 貼上工單 Google Sheet 的網址
   - 「指定分頁」留空（匯入全部船號）
   - 「工單號碼欄」填 `1`（A 欄）
   - 「備註欄」留空
4. 點選「**從 Google Sheet 匯入**」
5. 出現「匯入完成：新增 X 筆」即成功
6. 下方「現有工單清單」會列出所有已匯入的船號與工單

---

## 三、主管 — 日常操作

### 新增單一工單

適用於臨時新增一筆工單，不用更新整份 Google Sheet：

1. 工單管理 → 「新增工單」區塊
2. 填入「工單號碼」和「船號」（備註選填）
3. 點選「**新增**」

### 更新工單清單

當工單 Google Sheet 有新增資料時：

1. 工單管理 → 「從 Google Sheet 匯入工單」
2. 貼上同一份 Google Sheet 網址，點選「**從 Google Sheet 匯入**」
3. 系統會自動略過已存在的工單號碼，只新增新的

### 查詢派工記錄

1. 點選「**派工記錄**」分頁
2. 設定篩選條件（可任意組合）：
   - 施工日期範圍（起～迄）
   - 組別
   - 工號
   - 船號（文字輸入）
   - 工單號碼
   - 排序方式
3. 點選「**查詢**」，下方表格顯示結果

### 統計分析

1. 點選「**統計分析**」分頁
2. 設定日期範圍與組別（選填）
3. 點選「**統計**」，出現以下四個統計：
   - **依工單統計**：每個工單號碼出現幾筆
   - **依船號統計**：每艘船出現幾筆
   - **依組別統計**：每個組別出現幾筆
   - **依員工統計**：每個員工出現幾筆
   - **時間軸**：每日派工筆數長條圖（左右滑動查看）

---

## 四、員工 — 註冊與登入

### 註冊帳號

1. 開啟系統網址，點選「**員工註冊**」
2. 填入以下資料：
   - 工號（由公司指定，例如 `E001`）
   - 姓名
   - 組別（例如：`A組`、`北區組`）
   - 密碼（至少 6 個字元）
   - 確認密碼
3. 點選「**建立帳號**」
4. 成功後 3 秒自動跳轉至登入頁

> ⚠️ 工號一旦建立無法修改，請確認正確後再送出

### 登入

1. 開啟系統網址
2. 輸入工號與密碼
3. 點選「**登入**」
4. 登入後自動進入員工儀表板，導覽列顯示你的組別

---

## 五、員工 — 每日填寫派工

### 正常派工（工單在清單內）

1. 登入後，點選「**填寫報工**」分頁（預設開啟）
2. 確認或修改**施工日期**（預設今天）
3. 從「**船號**」下拉選單選擇今日施工的船
4. 下方出現該船的**工單勾選清單**，勾選今日施工的工單（可多選）
5. 點選「**送出報工**」
6. 出現「報工已儲存」即完成

### 臨時派工（工單不在清單內）

1. 施工日期照常選擇
2. 「船號」下拉選單拉到最底，選「**▶ 手動輸入臨時船號...**」
3. 輸入臨時船號
4. 輸入臨時工單號碼
5. 點選「**送出報工**」

### 混合情況（已知船號，臨時工單）

1. 從「船號」下拉選單選擇船號
2. 勾選清單中有的工單正常勾選
3. 在清單下方「**手動新增臨時工單號碼**」欄位輸入臨時工單
4. 一起送出（已勾選的工單 + 手動輸入的工單會合併記錄）

### 查看歷史記錄

1. 點選「**我的記錄**」分頁
2. 選擇年份和月份
3. 點選「**查詢**」
4. 表格顯示該期間所有派工記錄：施工日期、船號、工單號碼、組別

---

## 六、常見問題

**Q：船號下拉選單是空的？**
> 主管尚未匯入工單清單。請聯絡主管先完成工單匯入。

**Q：選了船號但工單清單沒有出現？**
> 該船號目前沒有對應的工單。可使用「手動輸入臨時船號」方式填寫，或請主管新增該船的工單。

**Q：忘記密碼怎麼辦？**
> 目前系統無自助重設密碼功能，請聯絡系統管理員在 Google Sheet 的 `Users` 分頁直接修改密碼欄位（密碼為 SHA-256 雜湊值，需重新雜湊後填入）。

**Q：送出報工後發現填錯了？**
> 目前系統無編輯功能，請聯絡主管在 Google Sheet 的 `Reports` 分頁直接修改或刪除該筆記錄。

**Q：主管後台看不到某員工的記錄？**
> 確認該員工是否已完成註冊，並確認「派工記錄」的篩選條件沒有不小心限縮範圍（例如日期範圍太窄）。

**Q：工單 Google Sheet 更新後系統沒有同步？**
> 需要手動重新匯入。前往「工單管理」→「從 Google Sheet 匯入工單」→ 重新貼上網址並匯入。

**Q：修改了 code.gs 但功能沒有更新？**
> 修改 GAS 程式碼後必須重新部署才會生效：Apps Script → 部署 → 管理部署 → 編輯 → 版本選「新版本」→ 部署。
